services:

  db:
    image: sql:8
    environment:
      MYSQL_DATABASE: 
      MYSQL_ROOT_PASSWORD: 

  prometheus:
    image: prom/prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
